# Workspace protocol

This package owns the portable first-party workspace contracts, built-in
component manifests, JSON-Schema prop validation, and deterministic state
reducer. It has no React or transport dependency.

The language-neutral contract is
`shared/schemas/v1/workspace.schema.json`; the published manifest registry is
`shared/registry/components.json`.
