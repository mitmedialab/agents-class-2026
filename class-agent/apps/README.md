# Applications

`web/` is the Phase 5 React/Vite Course Agent interface. It builds to ordinary
static assets and communicates with the versioned FastAPI routes over same-origin
HTTP. Browser-extension implementation remains deferred to its constitution
phase.
